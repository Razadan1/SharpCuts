@model List<BarberModel>

<h1>Barber List</h1>

@foreach (var barber in Model)
{
    <div>
        <img src="@barber.PictureUrl" alt="@barber.Name" width="100" height="100">
        <h3>@barber.Name</h3>
    </div>
}
    using SharpCuts.Models;
